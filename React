import { useState } from "react"; import { Button } from "@/components/ui/button"; import { Card, CardContent } from "@/components/ui/card";

export default function Loylypussit() { const [cart, setCart] = useState(0); const [lang, setLang] = useState("fi");

const addToCart = () => setCart(cart + 1); const toggleLang = () => setLang(lang === "fi" ? "en" : "fi");

const t = { fi: { title: "Koivulehtipussit saunaan", subtitle: "Perinteinen löylytuoksu luonnosta", buyNow: "Osta nyt", addToCart: "Lisää ostoskoriin", ourProducts: "Suosituimmat tuotteet", aboutTitle: "Meistä", aboutText: "Löylypussit on suomalainen brändi, joka tuo luonnon tuoksut suoraan saunaan. Käytämme aitoja koivunlehtiä ja ekologisia materiaaleja.", contactTitle: "Ota yhteyttä", email: "Sähköposti: info@loylypussit.fi", footer: "© 2025 Löylypussit. Kaikki oikeudet pidätetään." }, en: { title: "Birch Leaf Sachets for Sauna", subtitle: "Traditional natural sauna scent", buyNow: "Buy Now", addToCart: "Add to cart", ourProducts: "Most Popular Products", aboutTitle: "About Us", aboutText: "Löylypussit is a Finnish brand that brings the scents of nature directly to your sauna. We use real birch leaves and eco-friendly materials.", contactTitle: "Contact Us", email: "Email: info@loylypussit.fi", footer: "© 2025 Löylypussit. All rights reserved." } }[lang];

const redirectToCheckout = async () => { const response = await fetch("/create-checkout-session", { method: "POST", }); const data = await response.json(); window.location.href = data.url; };

return ( <main className="bg-beige min-h-screen text-green-900"> <header className="p-6 flex justify-between items-center shadow-md bg-white"> <h1 className="text-2xl font-bold flex items-center gap-2"> <span className="text-green-700">Löylypussit</span> </h1> <nav className="space-x-4 text-sm flex items-center"> <a href="#tuotteet">{lang === "fi" ? "Tuotteet" : "Products"}</a> <a href="#meista">{lang === "fi" ? "Meistä" : "About"}</a> <a href="#yhteys">{lang === "fi" ? "Ota yhteyttä" : "Contact"}</a> <Button onClick={toggleLang} className="ml-2">{lang.toUpperCase()}</Button> <span className="ml-4">🛒 {cart}</span> </nav> </header>

<section className="bg-white py-10 text-center">
    <h2 className="text-3xl font-semibold">{t.title}</h2>
        <p className="mt-2 text-lg">{t.subtitle}</p>
            <Button onClick={redirectToCheckout} className="mt-4 bg-green-700 text-white">{t.buyNow}</Button>
              </section>
              
                <section id="tuotteet" className="p-8 bg-beige">
                    <h3 className="text-2xl font-semibold mb-6">{t.ourProducts}</h3>
                        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                              {[9.9, 9.9, 9.3, 9.3].map((price, idx) => (
                                        <Card key={idx} className="bg-white">
                                                  <CardContent className="p-4">
                                                              <img
                                                                            src="/birch-pouch.jpg"
                                                                                          alt="Koivulehtipussi"
                                                                                                        className="w-full h-40 object-cover rounded"
                                                                                                                    />
                                                                                                                                <h4 className="mt-2 font-medium">Koivulehtipussi</h4>
                                                                                                                                            <p className="text-sm text-gray-700">€{price.toFixed(2)}</p>
                                                                                                                                                        <Button onClick={redirectToCheckout} className="mt-2 bg-green-700 text-white">{t.addToCart}</Button>
                                                                                                                                                                  </CardContent>
                                                                                                                                                                          </Card>
                              ))}
                                  </div>
                                    </section>

                                      <section id="meista" className="bg-white py-10 text-center">
                                          <h3 className="text-2xl font-semibold mb-2">{t.aboutTitle}</h3>
                                              <p className="max-w-xl mx-auto">{t.aboutText}</p>
                                                </section>

                                                  <section id="yhteys" className="bg-green-50 py-10 text-center">
                                                      <h3 className="text-2xl font-semibold mb-2">{t.contactTitle}</h3>
                                                          <p>{t.email}</p>
                                                            </section>

                                                              <footer className="text-center text-sm p-4 text-gray-500">
                                                                  {t.footer}
                                                                    </footer>
                                                                    </main>

); }

                              ))})}